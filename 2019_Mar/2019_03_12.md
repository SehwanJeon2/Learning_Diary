> # 2019.03.12

# 잡설

강사님 코드 모음

https://gist.github.com/eduyu

https://github.com/eduyu/django-sns



---

# sns 앱 추가 및 db 점검

```shell
$ django-admin startapp sns
```

 **manage.py 로 만드는 것과 무슨 차이?**

로컬에서는 `python manage.py startapp` 이 실행되지 않는다.



###  setting.py

```python
INSTALLED_APPS = [
    ...
    'sns',
]

# media fields
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```

상수를 생성

같은 상수 이름에서 settings와 다른 값이면 에러 나도록.

다른 프로그램은 대문자로 쓰면 강제로 상수 취급인데, 파이썬은 선택사항이다. 하지만 지키자.

media라는 디렉토리를 만들어서 사용자가 업로드 하는 사진을 받아 넣을 것이다.





### urls.py (project)

2가지를 추가로 import 해야한다.

`import static` 은

`import settings` 은 

경로 추가 및 파일을 주고 settings.py 를 인식시켜 주고(2번째 줄)

마지막에있는 `static(settings.MEDIA_URL, document_root=settings.Media_root)` 은 통째로 urlpatterns에 넣어도 된다. 하지만 path와 static을 쉽게 분류해서 보기 위해 따로 작성한 것이다.

```python
# upload setting
from django.conf.urls.static import static
from django.conf import settings

urlpatterns = [
    path('admin/', admin.site.urls),
    path('board/', include('board.urls')),
    path('sns/', include('sns.urls')),
]

# Dev Only
urlpatterns += static(settings.MEDIA_URL, document_root=settings.Media_root)
```



개발환경 상태에서는 디버그 페이지도 볼 수 있다.

개발 서버에서 media/ 파일들을 서빙 미지원

이건 개발 서버가 너무 경량화 상태인 것도 있다. 서버가 클라이언트에게 사진을 서빙하지 못 함.



디버그 펄스이면 빈 리스트를 보내준다. 베포에 알맞다????

개발에서도 베포에서도 모두 쓸 수 있다.



### urls.py

일단은 앱 이름만 설정하고 urlpatterns는 비워둔다.

urlpatterns를 아예 입력하지 않으면 migration 단계에서 에러가 발생한다.

```python
from django.urls import path
from . import views

app_name ='sns'

urlpatterns = [

]
```



### models.py

`ImageField` 를 사용하여 이미지 경로를 저장한다.

```python
from django.db import models

# Create your models here.

class Posting(models.Model):
    content = models.TextField()
    icon = models.CharField(max_length=20)
    # upload URL => /media/posting/origin/20190312/
    image = models.ImageField(blank=True, upload_to='posting/origin/%Y%m%d/')

    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return f'{self.id}: {self.content[:20]}'
```







url은 uniform resource locator이다. url은 브라우저만 해당 되는게 아님. 자원이 어디있는지 설명하는 것 뿐이다. 



한 폴더에 모든 리소스를 때려 넣는 것은 성능을 떨어트린다.

컴퓨터는 디렉토리가 많이 쪼개 분류한 상태에서 검색하는 걸 차라리 편하게 느낀다.



사진을 2개로 저장할 것이다. 원본과 썸네일 용.



처음 글을 게시할 때 업로드시간, 수정시간 모두 갱신.

글을 수정하면 수정시간만 갱신.



makemigrations 할 때

pillow를 설치해야하나??

이미 했지만 필요 없덴다.

아니 필요하다고?!?!



db에서 image는 실제 사진을 저장하는게 아니라 경로를 저장하는 것이다.



ㅇㅇㅇㅇ 방식은 아래와 같다.

컴퓨터는 그 이미지를 긁어서 변환해서 클라이언트에게 던져준다.

근데 사용자가 1000명이면 이런걸 1000번 해야해서 성능 뚝 떨어짐

그렇다고 암호화 된게 아니라서 보완도 그럼

암호화 시키는 것 자체는 가능하지만 복호과정에서 유추가 안 됨. ??? 그럼 쓰레기??

복호가 안되면 암호화가 아닌 것인가?







python shell_plus

p = Posting.objects.get

image는 imagefield file이 저장

image.ulr은 자동으로 생성된다. 우리가 입력한 적은 없어도.



같은 이미지를 저장하면 알아서 이름 바꾼다.



로컬에서 한 이유 : ......



### 이미지 resize가 필요

이미지 용량 제한이 필요하다. 언젠간 기가바이트 단위의 사진도 나올텐데, 이거 다 그대로 저장하고 송출하면 돈이 많이 깨진다. 로딩시간이 길어지는 것도 좋은 현상은 아니다.

단순히 작게만 보여주는 것은 데이터를 하나도 줄이기 못한다. 실제 데이터도 줄여서 보낼 수 있는 resize 작업이 필요하다.

근데 너무 줄이면 이미지 데이터가 증발할 수 있다.



1차 리사이즈는 적당하게 줄여주고

2차 리사이즈는 썸네일 수준으로 줄여야 한다.



### admin 암호화 이야기

강동주 강사님의 전문 분야

이더리움, 블록체인 등에 쓰이는 대표적 암호

요즘은 이 과정을 브라우저가 기본으로 해주지만, 원래 회원가입 기능이 있는 홈페이지는 기본으로 해야 하는 것이다. 일부 허접한 사이트는 이런 기본적인 암호도 쓰지 않으니 주의해라.



우리가 사용자 목록 DB에서 보는 해시는 매우 복잡한 숫자를 가진다. 이는 비밀번호를 암호화 알고리즘 120000 돌리는데 매번 솔트(salt)를 치면서 돌린다.

매우 복잡한 결과가 나오고 복호가 불가능하다. 하지만 복호가 불가능하다고해서 암호화 의미가 완전히 상실되는 것은 아니다.



한글로 빗대자면 부산 => ㅄ 으로 바꾼 셈.

그러면, 비수 => ㅄ 처럼 결과가 겹칠 수 있지 않는가?

다행히 위 방식이 매우 복잡하여 실제로 딱 중복되어 떨어지는 케이스가 없다.



---

# www 30주년 잡설타임

웹에도 세대가 있다. (위키 참고)

웹 1.0



웹 2.0

ucc



웹 3.0





이제는 창업해서 페이스북 같은 걸 만들기가 거의 불가능해졌다.

대기업이 보유하고있는 데이터와 승부해서 이기기 힘들기 때문이다.

따라서 데이터 양을 가지고 개인이 승리할 수 있는 시대는 저물어 갔다고 본다.



웹 개발자는 알고리즘은 거의 필요가 없다.

디자인 패턴만 공부하면 된다.

우리가 배웠던 mvc 패턴도 그 중 하나인데, 사실 조금 오래 된 것이다.



디자인 패턴이란?

A => B 로 간다고 치자. 경험적으로 최적의 길을 내서 해결해 나간게 디자인 패턴.

flask는 mvc는 없다. 하지만 flask로 대규모의 프로젝트를 다루면 결국엔 mvc로 나뉘어 구현하게 될 것이다.



---

# 사진 다루기

사진 크기 줄여서 저장



### models.py











### image-kit

```shell
$ pip install django-imagekit
```



upscale = False : 작은게 들어와도 크게 늘리지 않겠다.



resize to fill - 화면을 가득 채운다. 사진이 잘릴 수 있다.



resize to fit - 사진의 크기를 조절한다. 화면에 쏙 들어가게 하기 위해.





### 새 게시글 업로드 후 확인

960보다 큰 것은 잘려서 들어옴

960보다 작은 것은 그대로 들어옴



사진은 당장 저장된다.

하지만 썸네일 사진은 아직 만들지 않았다.

메소드를 호출하는 순간 썸네일 사진을 만든다.

shell plus로 만들어봤는데

분명 캐시에 썸네일 사진이 임시로 만들어진 것을 확인할 수 있다.

하지만 탐색기에서 찾을 수 없었다.





# 댓글 기능 추가







---

# urls.py 구현

urlpatterns만 채워넣는다.

```python
urlpatterns = [
    path('', views.posting_list, name='posting_list'),
    path('<int:posting_id>/', views.posting_detail, name='posting_detail'),
    path('create/', views.create_posting, name='create_posting'),
    path('<int:posting_id>/comment/create/', views.create_comment, name='create_comment'),
]
```



---

# views.py 구현



### posting_list

```python
def posting_list(request):
    postings = Posting.objects.all().order_by('-updated_at') # 마지막에 수정한 것이 맨 먼저오게
    return render(request, 'sns/list.html', {
        'postings': postings,
    })
```















# bootstrap

bootstrap에서 script 태그는 head가 아니라 body에 넣는게 정석

java script 는 그렇다

왜냐하면 head 그거 읽어오기 전에 아무것도 안함. 페이지 완전 로드 되기전에 아무것도 안 띄우는 듯 하다.

이용자 입장에선 페이지가 느려보임



### base.html

```html
<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Fakebook</title>
</head>
<body>
    <div class="container">
        {% block body %}
        {% endblock %}
    </div>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
</body>
</html>
```





### list.html

card 형식으로 구현

```html
{% extends 'sns/base.html' %}

{% block body %}
{% if postings %}
    <section class="card-columns">
        {% for posting in postings %}
            <article class="card mb-2">
                <a href="{% url 'sns:posting_detail' posting.id %}">
                    <img src="{{ posting.image_thumbnail.url }}"
                        class="card-img-top image-field"
                        alt="posting.image">
                </a>
                <div class="card-body">
                    <i>{{ posting.icon }}</i>
                </div>
            </article>
        {% endfor %}
    </section>
{% endif %}
{% endblock %}
```

`<i>` 태그는 글자를 기울여서 표시하는 태그로, italic의 약자이다.







p.card-text + tab

### detail.html







### base.html 수정



```html
ul.nav.nav-tabs>li.nav-item>a
```



```html
<ul class="nav nav-tabs">
    <li class="nav-item"><a href=""></a></li>
</ul>
```









컨트롤 + d 줄 복사

컨트롤 backspace => 단어 단위로 제거



컨트롤 쉬프트 위, 아래 => 1줄 위, 아래로 이동



파일 업로드 할거면 반드시 써야 하는 명령이 있다.



```html
    <form action="{% url 'sns:create_posting' %}" method="POST" enctype="multipart/form-data">
```





db 객체 만드는 명령어가 또 있음

```python
def create_posting(request):
    if request.method == 'POST':
        posting = Posting.objects.create(
            content = request.POST.get('content'),
            icon = request.POST.get('icon'),
            image = request.FILES.get('image'),
        )
        # 요청이 밀린 경우 라스트에 누가 나올지 몰라서 if 안에 집어넣음.
        return redirect('sns:posting_detail', posting.id)
    else:
        return redirect('sns:posting_detail')
```



### 이유 없는 에러 발생

새로고침하면 어떨 땐 에러 어떨 땐 정상

DB 에서 꼬인 듯



서버 내린 후

db를 다시 지우고 다시 만들어보자

```shell
$ python manage.py migrate sns zero # 기존 db 지우기
$ rm -rf media/   # 기존 저장된 이미지 지우기
$ python manage.py migrate  # 다시 만들기
```

서버 다시 올린다.



혹은 사진 로딩이 안되면 랜덤 이미지로 대체

---





### font awesome

font awesome으로 아이콘이 잘 안 만들어져서 보류

css 경로 추가



